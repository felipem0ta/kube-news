<!doctype html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="">
  <title>Novo Post</title>

  <%- include('./partial/bootstrap-css'); %>

    <style>
      .bd-placeholder-img {
        font-size: 1.125rem;
        text-anchor: middle;
        -webkit-user-select: none;
        -moz-user-select: none;
        user-select: none;
      }

      @media (min-width: 768px) {
        .bd-placeholder-img-lg {
          font-size: 3.5rem;
        }
      }

      .container {
        max-width: 960px;
      }
    </style>
</head>

<body class="bg-light">

  <%- include('./partial/nav-bar'); %>

    <div class="container">
      <main>
        <div class="py-2 text-center">
          <h2>Novo Recado</h2>
        </div>

        <div class="row g-5">
          <div class="col-md-12 col-lg-12">
            <h4 class="mb-3"></h4>
            <form class="needs-validation" action="/post" method="post" novalidate>
              <div class="row g-3">
                <div class="col-12">
                  <label for="title" class="form-label">Título: <span class="text-muted"></span></label>
                  <input type="text" class="form-control" id="title" name="title" placeholder="Título"
                    pattern=".{1,30}"
                    required>
                  <div class="invalid-feedback">
                    Esse campo é obrigatório e deve ter no máximo 30 caracteres
                  </div>
                </div>
                <div class="col-12">
                  <label for="resumo" class="form-label">Resumo: <span class="text-muted"></span></label>
                  <input type="text" class="form-control" id="resumo" name="resumo" placeholder="Opcional"
                    pattern=".{1,100}">
                </div>
                <div class="col-12">
                  <label for="address" class="form-label">Recado:</label>
                  <textarea class="form-control" id="address" name="description" rows="10" required></textarea>
                  <div class="invalid-feedback">
                    Esse campo é obrigatório
                  </div>
                </div>
              </div>
              <br />
              <button class="w-100 btn btn-primary btn-lg btn-dark" type="submit">Salvar</button>
            </form>
          </div>
        </div>
      </main>
    </div>

    <%- include('./partial/bootstrap-js'); %>
    <script src="/assets/dist/js/form-validation.js"></script>
</body>

</html>